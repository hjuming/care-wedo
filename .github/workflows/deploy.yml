name: Deploy to Cloudflare Pages

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      deployments: write

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: care-wedo-app/package-lock.json

      - name: Install dependencies
        run: npm ci
        working-directory: care-wedo-app

      - name: Build
        run: npm run build
        working-directory: care-wedo-app
        env:
          VITE_LINE_LIFF_ID: ${{ secrets.VITE_LINE_LIFF_ID }}

      - name: Deploy to Cloudflare Pages
        run: npx wrangler@4 pages deploy care-wedo-app/dist --project-name care-wedo --branch main
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
